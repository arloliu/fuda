# Configuration with external references
app_name: "secrets-example"

# Path to database password (used by refFrom)
db_password_path: "file://secrets/db_password.txt"

# TLS configuration with dynamic paths
tls:
  cert_path: "file://secrets/server.crt"
  key_path: "file://secrets/server.key"
