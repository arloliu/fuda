# fuda-doc

Generate Markdown documentation from a tagged configuration struct.

```bash
go install github.com/arloliu/fuda/cmd/fuda-doc@latest
fuda-doc --struct Config --path ./internal/config --markdown --output CONFIG.md
```

Read the [fuda-doc guide](../../docs/tooling/fuda-doc.md) for every supported mode and flag.
